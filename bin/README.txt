ensemble:
	所有绘图，显示，评估， 表格等界面
	control:
		界面特性控制，如点击响应，搜索等功能
	generated:
		案例包。也就是各个图案的实例对象，通过sample传递到界面从而显示
	playground:
		坐标轴属性控制
	samplepage:
		案例显示特性，也就是各个组成部分，如坐标轴，图像，参数输入，各项功能等
		description:
			包含绘图，图片保存，评估，表格等功能
	
	Boc_***:
		Boc特性曲线，如时域，频域，自相关等
	Bpsk_***:
		Bpsk特性曲线，如时域，频域，自相关等
	CurveFittedAreaChart:
		图形渲染
	EnsembleApp:
		主活动
	CurveS/Tracking_Error/MultiPath:
		S曲线，码跟踪误差，多径
login:
	登陆界面
stagecontroller:
	控制界面切换（登陆界面切换到主界面）